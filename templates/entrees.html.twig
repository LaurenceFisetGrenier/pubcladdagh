<!doctype html>
<!--[if lt IE 7]>      <html class="no-js lt-ie9 lt-ie8 lt-ie7" lang=""> <![endif]-->
<!--[if IE 7]>         <html class="no-js lt-ie9 lt-ie8" lang=""> <![endif]-->
<!--[if IE 8]>         <html class="no-js lt-ie9" lang=""> <![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js" lang="fr"> <!--<![endif]-->
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <link rel="apple-touch-icon" href="apple-touch-icon.png">
        <link rel="stylesheet" href="{{ niveau }}css/main.css">

        <script src="{{ niveau }}js/vendor/modernizr.js"></script>


        <title>Entres | Claddagh</title>
    </head>
    <body>
        <!--[if lt IE 8]>
            <p class="browserupgrade">You are using an <strong>outdated</strong> browser. Please <a href="http://browsehappy.com/">upgrade your browser</a> to improve your experience.</p>
        <![endif]-->
        {{ include("header.html.twig") }}
        {% if erreur is not empty %}
            <p>{{ erreur }}</p>
        {% else %}

        <main>
            <h1 class="prog">Nos entrées</h1>
            <a href="index.php" id="lien-retour">Retour aux choix de menus</a>
            {% for entree in entrees %}
                <p class="nom-item">{{ entree.nom_plat }}</p>
                <p class="description-item">{{ entree.description_plat }}</p>
                <p class="description-prix-item">{{ entree.description }}</p>
                <p class="prix-item">{{ entree.prix }}</p>
            {% endfor %}
            <h2>Smoked meat</h2>
            {% for meat in meats %}
                <p class="nom-item">{{ meat.nom_plat }}</p>
                <p class="description-item">{{ meat.description_plat }}</p>
                <p class="description-prix-item">{{ meat.description }}</p>
                <p class="prix-item">{{ meat.prix }}</p>
            {% endfor %}
           
        </main> <!-- #main-container -->


        {% endif %}
        {{ include("footer.html.twig") }}
    </body>
</html>
