<!DOCTYPE HTML>
<!--[if lt IE 7]>      <html class="no-js lt-ie9 lt-ie8 lt-ie7" lang=""> <![endif]-->
<!--[if IE 7]>         <html class="no-js lt-ie9 lt-ie8" lang=""> <![endif]-->
<!--[if IE 8]>         <html class="no-js lt-ie9" lang=""> <![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js" lang="fr"> <!--<![endif]-->
<html lang="fr">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width">
	<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
	<meta name="description" content="">

	<link rel="stylesheet" href="{{ niveau }}css/main.css">
	<link rel="icon" type="image/png" href="{{ niveau }}images/icone.png" />
    <link rel="icon" type="image/png" href="{{ niveau }}images/favicon.png" />

	<script src="{{ niveau }}js/vendor/modernizr.js"></script>

	<title>Whiskey | Claddagh</title>
</head>
<body>
	<!--[if lt IE 8]>
            <p class="browserupgrade">You are using an <strong>outdated</strong> browser. Please <a href="http://browsehappy.com/">upgrade your browser</a> to improve your experience.</p>
        <![endif]-->
	{{ include("header.html.twig") }}
	{% if erreur is not empty %}
			<p>{{ erreur }}</p>
		{% else %}

	<main id="page-whiskey">

		<h1 class='prog'>Nos whiskey</h1>
		<a href="index.php" id="lien-retour">Retour aux choix de menus</a>

        <ul class="nav-menus">
            <li>
                <a href="bieres.php">Bières</a>
                <a href="vins.php">Vins</a>
                <a href="#" class="menu-actif">Whiskey</a>
            </li>
        </ul>

		 <h2>Irlande</h2>
                {% for irlande in irlandes %}
                    <div class="menu">
                        <p class="nom-item">{{ irlande.nom_plat }}</p>
                        <p class="description-prix-item">{{ irlande.description }}</p>
                        <p class="prix-item">{{ irlande.prix }} $</p>
                        <!--<p class="description-item">{{ irlande.description_plat }}</p>-->
                    </div>
                {% endfor %}
                <h2>États-Unis</h2>
                {% for etat in etats %}
                    <p class="nom-item">{{ etat.nom_plat }}</p>
                    <p class="description-prix-item">{{ etat.description }}</p>
                    <p class="prix-item">{{ etat.prix }} $</p>
                    <!--<p class="description-item">{{ etat.description_plat }}</p>-->
                {% endfor %}
                <h2>Écosse</h2>
                {% for ecosse in ecosses %}
                    <p class="nom-item">{{ ecosse.nom_plat }}</p>
                    <p class="description-prix-item">{{ ecosse.description }}</p>
                    <p class="prix-item">{{ ecosse.prix }} $</p>
                    <!--<p class="description-item">{{ ecosse.description_plat }}</p>-->
                {% endfor %}
	</main>

	{% endif %}
	{{ include("footer.html.twig") }}

</body>
</html>